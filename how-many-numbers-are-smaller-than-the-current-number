class Solution:
    def smallerNumbersThanCurrent(self, nums: List[int]) -> List[int]:
        c_arr = []
        count = 0
        for i in nums:
            for j in nums:
                if i > j:
                    count += 1
            c_arr.append(count)
            count = 0
        return c_arr
        
