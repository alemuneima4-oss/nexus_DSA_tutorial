class Solution:
    def similarPairs(self, words: List[str]) -> int:
        counter = 0
        s = 1
        for i in words:
            for j in words[s:]:
                if set(i) == set(j):
                    counter += 1
            s += 1
        return counter
        
