if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr = list(arr)
    max_arr = max(arr)
    arr.remove(max(arr))
    while True:
        if max_arr > max(arr):
            print(max(arr))
            break
        else:
            arr.remove(max(arr))
            continue
